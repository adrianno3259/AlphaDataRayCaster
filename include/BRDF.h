#ifndef BRDF_H
#define BRDF_H

/*
class Intersection;

class BRDF
{
    public:
        BRDF(float diffuseCoef, Color color);
        virtual ~BRDF();

        Color f(const Vec3d& wo,
                const Vec3d& wi,
                Intersection& it) const;

        Color f(const Vec3d& wo,
                const Vec3d& wi) const;

    protected:
        double mDiffuseCoef;
        Color  mColor;
    private:
};
      */
#endif // BRDF_H
